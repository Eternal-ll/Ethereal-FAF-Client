<UserControl x:Class="beta.Views.SessionAuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:beta.ViewModels"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             d:DataContext="{d:DesignInstance {x:Type vm:SessionAuthorizationViewModel}}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ui:ProgressRing IsActive="True" Height="100" Width="100" Margin="30 0"/>
            <TextBlock HorizontalAlignment="Left" Text="{Binding CurrentState}" Margin="10 30 0 0" Opacity=".3"/>
        </StackPanel>
    </Grid>
</UserControl>
