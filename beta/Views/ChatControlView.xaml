<UserControl x:Class="beta.Views.ChatControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:control="clr-namespace:beta.Resources.Controls"
             xmlns:vm="clr-namespace:beta.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance {x:Type vm:ChatControlViewModel}}">
    <!--<UserControl.DataContext>
        <vm:ChatControlViewModel/>
    </UserControl.DataContext>-->
    <control:CrossFadeContentControl Content="{Binding CurrentViewModel}"/>
</UserControl>
