namespace Ethereal.FAF.UI.Client.Infrastructure.Lobby
{
    public enum ConnectionState
    {
        NotConnected,
        Connecting,
        Connected,
        Disconnected,
        Disconnecting,
    }
}
