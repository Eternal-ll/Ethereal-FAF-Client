<local:PageBase x:Class="Ethereal.FAF.UI.Client.Views.LobbyConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ethereal.FAF.UI.Client.Views"
                d:DataContext="{d:DesignInstance {x:Type local:LobbyConnectionView}}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Connect" Command="{Binding ViewModel.ConnectCommand}"/>
            <Button Content="Disconnect" Command="{Binding ViewModel.DisconnectCommand}"/>
            <Button Content="Back" Command="{StaticResource NavigateRootViewCommand}"
                    CommandParameter="{x:Type local:AuthView}"/>
        </StackPanel>
    </Grid>
</local:PageBase>
